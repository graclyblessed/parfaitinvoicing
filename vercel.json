{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 8 * * *"
    }
  ]
}
